Environment setup:

uv venv -p 3.11
uv pip install -r requirements.txt
uv pip install torch==2.0.0 torchvision==0.15.1 --index-url https://download.pytorch.org/whl/cu118 --force-reinstall
uv pip install -e ../tlc-monorepo[pacmap]
uv pip install setuptools wheel "numpy<2" ipykernel
uv pip install -e ../detectron2 --no-build-isolation
export TORCH_CUDA_ARCH_LIST="8.6"
cd maskdino/modeling/pixel_decoder/ops
sh make.sh
cd ../../../..


MODEL.ROI_HEADS.NUM_CLASSES: 1
MODEL.SEM_SEG_HEAD.NUM_CLASSES: 1
MODEL.MaskDINO.NUM_OBJECT_QUERIES: 10
TEST.DETECTIONS_PER_IMAGE: 10

https://github.com/gudbrandtandberg/MaskDINO.git